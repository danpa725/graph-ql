type Movie {
    id: Int!
    title: String!
    rating: Float!
    summary: String!
    language: String!
    medium_cover_image: String
}

type Query {
    movies(limit: Int, rating: Float): [Movie]!
    movie(title: String!): Movie
}

##! 보내는 정보 QUERY
